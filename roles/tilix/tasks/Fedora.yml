---
# tasks file for default-tooling

- name: Install default packages
  dnf: 
    name: "{{ item }}"
    state: present
  with_items:
    - "{{ tilix_packages | map(attribute='dnf') | list }}"
  tags:
    - dnf
    - download
    - packages
  become: true
